import { PlaceholderPage } from "@/features/dashboard/placeholder-page";

export default function ExecutionsPage() {
  return (
    <PlaceholderPage
      description="Trace every step, model call, tool handoff, event, and attestation across workflow runs."
      eyebrow="Execution Logs"
      title="Every action, accounted for."
    />
  );
}
