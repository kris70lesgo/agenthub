# `@agenthub/casper`

Browser-safe Casper SDK services for AgentHub wallet state, AgentHub Registry
contract-call payloads, workflow attestations, agent identity, transaction
history, and reputation.

Phase 6 keeps writes compact: only hashes, versions, timestamps, identity
metadata, reputation scores, and execution counts are prepared for Casper.
